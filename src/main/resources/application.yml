spring:
  datasource:
    url: jdbc:mysql://localhost:3306/community?allowPublicKeyRetrieval=true&serverTimezone=UTC&useSSL=false
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

server:
  servlet:
    context-path: /api/v1

logging:
  level:
    root: WARN
    kr.adapterz: INFO
    org.hibernate.SQL: INFO
  exception-conversion-word: '%ex{short}'